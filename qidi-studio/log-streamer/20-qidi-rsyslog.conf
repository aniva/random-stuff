# rsyslog configuration for QIDI 3D Printer
# Allows remote syslog forwarding from QIDI Linux SoC (192.168.1.27) to ai-box

if $fromhost-ip == '192.168.1.27' then {
    action(type="omfile" file="/var/log/remote/qidi/syslog.log")
    stop
}
